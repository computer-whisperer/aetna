//! Aetna — backend-agnostic UI library core.
//!
//! Scaffolded as v5.0 step 1. Subsequent commits port the v0.4 source
//! (`attempts/attempt_4/`) into this crate, then refactor the `El`
//! bookkeeping fields off the tree into `UiState` side maps.
//!
//! See `V5.md` at the repo root for the slice-by-slice plan.
