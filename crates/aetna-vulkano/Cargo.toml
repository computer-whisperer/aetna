[package]
name = "aetna-vulkano"
version = "0.1.0"
edition = "2024"
description = "Aetna — Vulkan backend via vulkano + naga WGSL→SPIR-V (native only)"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
aetna-core = { path = "../aetna-core" }
