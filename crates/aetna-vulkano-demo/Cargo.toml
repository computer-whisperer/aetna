[package]
name = "aetna-vulkano-demo"
version = "0.1.0"
edition = "2024"
description = "Aetna — vulkano native winit harness and backend parity tools"
license = "MIT OR Apache-2.0"
publish = false

[lib]
path = "src/lib.rs"

# Binaries auto-discovered from `src/bin/*.rs`.

[dependencies]
aetna-core = { path = "../aetna-core" }
aetna-fixtures = { path = "../aetna-fixtures" }
aetna-vulkano = { path = "../aetna-vulkano" }
png = "0.18"
vulkano = "0.35"
winit = "0.30"
